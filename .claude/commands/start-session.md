---
description: Resume work on the Al Mansour gym landing page with full project context
---

You are resuming work on the **Al Mansour gym landing page** (HTML + Tailwind CSS + vanilla JS, no backend).

Before doing or saying anything else:

1. Read `memory.md` in full. This holds the locked-in design tokens, typography,
   file map, section order, and scope boundaries. Treat it as ground truth —
   do not re-decide anything it already answers.
2. Read `journal.md` in full, especially the most recent session entry, to see
   exactly what was already built and what was left outstanding.
3. Skim the current state of `index.html`, `assets/css/style.css`,
   `assets/js/main.js`, and `assets/js/config.js` to confirm the files on disk
   match what the journal claims — if they don't match, say so before proceeding.
4. Summarize back to the user, in a few short lines: what's done, what's
   outstanding (pull from the journal's "Outstanding" list), and what you
   propose to work on this session.
5. Only after that summary, continue work — staying inside the scope defined
   in `memory.md` §5 and §8 (six sections only, no backend, no cart, no form).

Do not restate the entire memory/journal file back to the user — summarize.
