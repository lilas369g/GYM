---
description: Close out a work session on the Al Mansour gym landing page by writing everything down
---

You are closing out a work session on the **Al Mansour gym landing page**.

Do the following, in order, before ending the session:

1. Re-read the current `journal.md` so you don't duplicate an existing entry.
2. Append a **new** dated session entry (never edit or delete previous entries)
   covering, in the same structure as prior entries:
   - What the Frontend Developer persona built or changed this session.
   - What the E2E Tester persona checked (list the breakpoints/devices tested)
     and any bugs found + whether they were fixed.
   - What the Judge persona ruled — approved, or sent back with specific
     scope/quality reasons.
   - An updated "Outstanding" list — remove anything finished, add anything
     newly discovered.
3. If any decision this session changed something documented in `memory.md`
   (a color, a section, a file path, the scope), update `memory.md` directly
   so it stays the single source of truth — do not leave the change only in
   the journal.
4. Double-check nothing was left half-written: any file you edited should be
   in a working, non-broken state before you stop.
5. Confirm to the user, in a few short lines, what was written to `journal.md`
   and (if applicable) what changed in `memory.md`.
